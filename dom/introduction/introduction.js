
// function printName() {
//     console.log(window);
// }

/*
    To do any changes with in the webpage dynamically , we need the access to that particular HTML element object.

    document API / object present inside the window (globally) is the one knows everything about DOM tree.

    doucment API has so many methods that will give us the desired HTML element Object.


    1. Access to nodes ( html element object ) => Reading objects from DOM tree. => Read
    2. Manipulating a DOM tree node.(changing the behaviour of the nodes) => Update
    3. Deleting a node from the DOM tree. => Delete
    4. Adding / creating a new node into the DOM tree => Create

    CRUD


    Events topic

    Employee management 
    Jira dashboard clone
    Google sheet clone
    Excalidraw

*/